{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range .Data.Pages }}
    {{- $robots := .Params.robots | default "" }}
    {{- $isNoIndex := strings.Contains $robots "noindex" }}
    {{- $isTaxonomy := in (slice "taxonomy" "term") .Kind }}
    {{- if and (not $isNoIndex) (not $isTaxonomy) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    {{- else if not .Date.IsZero }}
    <lastmod>{{ .Date.Format "2006-01-02" }}</lastmod>
    {{- end }}
    {{- if .IsHome }}
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
    {{- else if strings.Contains .Permalink "/courses/" }}
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
    {{- else if strings.Contains .Permalink "/insights/" }}
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
    {{- else if or (strings.Contains .Permalink "/assessment/") (strings.Contains .Permalink "/who-we-serve/") }}
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    {{- else }}
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
    {{- end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
